import React from "react";

const IndividualBooking = props => {
  const { bookings } = props;
  return <div>This is a booking, if there is one at all... </div>;
};

export default IndividualBooking;
